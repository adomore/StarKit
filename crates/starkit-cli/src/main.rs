//! starkit — CLI. Implemented in Phase 1 (task T1-8). See ROADMAP.md.

fn main() {
    eprintln!("starkit: scaffold only — the CLI arrives in Phase 1 (task T1-8). See ROADMAP.md.");
    // Placeholder exit code; the stable exit-code table is defined in T1-8.
    std::process::exit(64);
}
