//! starkit-fixtures — synthetic golden star-field generator (task T0-1).
//!
//! Spec: docs/FIXTURES.md. Must not depend on starkit-core (non-circularity rule).

fn main() {
    eprintln!("starkit-fixtures: not yet implemented — start here (task T0-1, spec in docs/FIXTURES.md).");
    std::process::exit(64);
}
